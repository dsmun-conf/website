extends /layouts/base.pug

append stylesheets
    if stylesheets != null
        for stylesheet in stylesheets
            link(rel="stylesheet" href=stylesheet)

block content
    article
        | !{content}
